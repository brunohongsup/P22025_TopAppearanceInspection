public:

#pragma region [CRavidLine]
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	CRavidLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	CRavidLine(_In_ T a, _In_ T b, _In_ T c);
	CRavidLine(_In_ const POINT& point);
	CRavidLine(_In_ POINT* pPoint);
	CRavidLine(_In_ const SIZE& size);
	CRavidLine(_In_ SIZE* pSize);
	CRavidLine(_In_ const CRavidPoint<int>& point);
	CRavidLine(_In_ CRavidPoint<int>* pPoint);
	CRavidLine(_In_ const CRavidPoint<long long>& point);
	CRavidLine(_In_ CRavidPoint<long long>* pPoint);
	CRavidLine(_In_ const CRavidPoint<float>& point);
	CRavidLine(_In_ CRavidPoint<float>* pPoint);
	CRavidLine(_In_ const CRavidPoint<double>& point);
	CRavidLine(_In_ CRavidPoint<double>* pPoint);
	CRavidLine(_In_ const CRavidPoint<int>& rpCenter, _In_ double dblAngle);
	CRavidLine(_In_ CRavidPoint<int>* pRpCenter, _In_ double dblAngle);
	CRavidLine(_In_ const CRavidPoint<long long>& rpCenter, _In_ double dblAngle);
	CRavidLine(_In_ CRavidPoint<long long>* pRpCenter, _In_ double dblAngle);
	CRavidLine(_In_ const CRavidPoint<float>& rpCenter, _In_ double dblAngle);
	CRavidLine(_In_ CRavidPoint<float>* pRpCenter, _In_ double dblAngle);
	CRavidLine(_In_ const CRavidPoint<double>& rpCenter, _In_ double dblAngle);
	CRavidLine(_In_ CRavidPoint<double>* pRpCenter, _In_ double dblAngle);
	CRavidLine(_In_ const CRavidPoint<int>& rpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ CRavidPoint<int>* pRpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ const CRavidPoint<long long>& rpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ CRavidPoint<long long>* pRpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ const CRavidPoint<float>& rpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ CRavidPoint<float>* pRpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ const CRavidPoint<double>& rpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ CRavidPoint<double>* pRpCenter, _In_ double dblAngle, _In_ double dblLineLength);
	CRavidLine(_In_ const POINT& point1, _In_ const POINT& point2);
	CRavidLine(_In_ POINT* pPoint1, _In_ POINT* pPoint2);
	CRavidLine(_In_ const CRavidPoint<int>& point1, _In_ const CRavidPoint<int>& point2);
	CRavidLine(_In_ CRavidPoint<int>* pPoint1, _In_ CRavidPoint<int>* pPoint2);
	CRavidLine(_In_ const CRavidPoint<long long>& point1, _In_ const CRavidPoint<long long>& point2);
	CRavidLine(_In_ CRavidPoint<long long>* pPoint1, _In_ CRavidPoint<long long>* pPoint2);
	CRavidLine(_In_ const CRavidPoint<float>& point1, _In_ const CRavidPoint<float>& point2);
	CRavidLine(_In_ CRavidPoint<float>* pPoint1, _In_ CRavidPoint<float>* pPoint2);
	CRavidLine(_In_ const CRavidPoint<double>& point1, _In_ const CRavidPoint<double>& point2);
	CRavidLine(_In_ CRavidPoint<double>* pPoint1, _In_ CRavidPoint<double>* pPoint2);
	CRavidLine(_In_ const CRavidLine<int>& line);
	CRavidLine(_In_ CRavidLine<int>* pLine);
	CRavidLine(_In_ const CRavidLine<long long>& line);
	CRavidLine(_In_ CRavidLine<long long>* pLine);
	CRavidLine(_In_ const CRavidLine<float>& line);
	CRavidLine(_In_ CRavidLine<float>* pLine);
	CRavidLine(_In_ const CRavidLine<double>& line);
	CRavidLine(_In_ CRavidLine<double>* pLine);
	CRavidLine(_In_ const RECT& rect);
	CRavidLine(_In_ RECT* pRect);
	CRavidLine(_In_ const CRavidRect<int>& rect);
	CRavidLine(_In_ CRavidRect<int>* pRect);
	CRavidLine(_In_ const CRavidRect<long long>& rect);
	CRavidLine(_In_ CRavidRect<long long>* pRect);
	CRavidLine(_In_ const CRavidRect<float>& rect);
	CRavidLine(_In_ CRavidRect<float>* pRect);
	CRavidLine(_In_ const CRavidRect<double>& rect);
	CRavidLine(_In_ CRavidRect<double>* pRect);
#pragma endregion

#pragma region [SetLine]
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ int p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ long long p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ float p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ int p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ long long p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ float p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ int p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ long long p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ float p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ int p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ long long p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ float p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] p1x : 1번째 x 좌표
	* param[in] p1y : 1번째 y 좌표
	* param[in] p2x : 2번째 x 좌표
	* param[in] p2y : 2번째 y 좌표
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ double p1x, _In_ double p1y, _In_ double p2x, _In_ double p2y);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry1 : 1번째 x, y 좌표 (Reference)
	* param[in] refGeometry2 : 2번째 x, y 좌표 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ POINT& refGeometry1, _In_ POINT& refGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry1 : 1번째 x, y 좌표 (Pointer)
	* param[in] pGeometry2 : 2번째 x, y 좌표 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ POINT* pGeometry1, _In_ POINT* pGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry1 : 1번째 x, y 좌표 (Reference)
	* param[in] refGeometry2 : 2번째 x, y 좌표 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<int>& refGeometry1, _In_ CRavidPoint<int>& refGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry1 : 1번째 x, y 좌표 (Pointer)
	* param[in] pGeometry2 : 2번째 x, y 좌표 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<int>* pGeometry1, _In_ CRavidPoint<int>* pGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry1 : 1번째 x, y 좌표 (Reference)
	* param[in] refGeometry2 : 2번째 x, y 좌표 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<long long>& refGeometry1, _In_ CRavidPoint<long long>& refGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry1 : 1번째 x, y 좌표 (Pointer)
	* param[in] pGeometry2 : 2번째 x, y 좌표 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<long long>* pGeometry1, _In_ CRavidPoint<long long>* pGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry1 : 1번째 x, y 좌표 (Reference)
	* param[in] refGeometry2 : 2번째 x, y 좌표 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<float>& refGeometry1, _In_ CRavidPoint<float>& refGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry1 : 1번째 x, y 좌표 (Pointer)
	* param[in] pGeometry2 : 2번째 x, y 좌표 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<float>* pGeometry1, _In_ CRavidPoint<float>* pGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry1 : 1번째 x, y 좌표 (Reference)
	* param[in] refGeometry2 : 2번째 x, y 좌표 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<double>& refGeometry1, _In_ CRavidPoint<double>& refGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry1 : 1번째 x, y 좌표 (Pointer)
	* param[in] pGeometry2 : 2번째 x, y 좌표 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<double>* pGeometry1, _In_ CRavidPoint<double>* pGeometry2);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<int>& refGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<int>* pGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<long long>& refGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<long long>* pGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<float>& refGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<float>* pGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<double>& refGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다. 무한한 직선을 생성합니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<double>* pGeometry, _In_ double dblAngle);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<int>& refGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<int>* pGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<long long>& refGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<long long>* pGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<float>& refGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<float>* pGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 선의 중심 (Reference)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<double>& refGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 선의 중심 (Pointer)
	* param[in] dblAngle : 회전 각도
	* param[in] dblLineLength : 선의 길이
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidPoint<double>* pGeometry, _In_ double dblAngle, _In_ double dblLineLength);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 2좌표를 가지는 Line 객체 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<int>& refGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 2좌표를 가지는 Line 객체 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<int>* pGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 2좌표를 가지는 Line 객체 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<long long>& refGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 2좌표를 가지는 Line 객체 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<long long>* pGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 2좌표를 가지는 Line 객체 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<float>& refGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 2좌표를 가지는 Line 객체 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<float>* pGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] refGeometry : 2좌표를 가지는 Line 객체 (Reference)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<double>& refGeometry);
	/*
	* 현재 객체를 설정하는 기능입니다.
	*
	* param[in] pGeometry : 2좌표를 가지는 Line 객체 (Pointer)
	*
	* return value : 실패하면 false 를 반환합니다.
	*/
	bool SetLine(_In_ CRavidLine<double>* pGeometry);
#pragma endregion